// 19. Напишите функцию, которая сортирует массив по убыванию


const arr = [3, 8, 7, 6, 5, -4, 3, 2, 1];

function sort(incomingArray) {
    return incomingArray.sort();
}

console.log(sort(arr)); // [-4,1,2,3,3,5,6,7,8]